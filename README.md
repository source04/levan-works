# levan.works

Personal portfolio — design implemented from Figma.

## Run locally

```bash
npm run dev
```

Then open **http://localhost:3000** in your browser.

You can also open `index.html` directly in a browser (no server required).
